# The directory containing the Node binary used to run this checkout.
PATH=/home/you/.nvm/versions/node/v24.18.1/bin:/usr/local/bin:/usr/bin

# Generate a dedicated value, for example: openssl rand -hex 32
GATEWAY_WEB_AUTH_TOKEN=replace-with-a-random-secret
