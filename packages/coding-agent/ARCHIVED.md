# 归档声明（请勿修改、请勿使用）

> 状态：**冻结**。本包仅作为参考代码保留。

## 不要修改

- 本包的 `src/`、`test/`、`scripts/` 等任何代码**一律不要改动**，包括修复 lint、
  类型错误或安全告警。它不参与 `npm run build` 与发布流程，改动只会带来噪音。
- 根 `README.md` 已声明：本包"保留在仓库中作为参考代码，已不再被任何模块引用"。
- 若 `npm run check` 或测试输出中涉及本包的预存失败，属于预期现象，不在任何
  改进计划的范围内，不要为了它修改本包。

## 不要使用

- 不要在本包之外 `import`、引用或依赖它的任何导出；不要把它接入 chat/host/
  gateway/proactive/drift 等运行链路。
- 本包内含一份与 `packages/host/src/core/memory/` 同源的记忆引擎拷贝，二者已
  漂移；任何新功能请以 host 版为准。

## 未来

本包是否恢复为正式产品形态（例如重新作为 CLI 编码代理使用），由仓库所有者
另行决定。在此之前，它保持冻结状态。
